import { ResourcesContent } from "@/components/resources-content"

export const metadata = {
  title: "Resources",
  description: "Free tools to help you move with confidence — printable guides, home safety checklists, beginner balance exercises, videos, and answers to common questions about BALNZ.",
}

export default function ResourcesPage() {
  return <ResourcesContent />
}